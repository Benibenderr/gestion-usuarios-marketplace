# Copiar como .env solo si querés apuntar a una API distinta a la local.
# Si no existe este archivo, el frontend usa /api y el proxy de Vite
# reenvía las llamadas a http://localhost:3001 (ver vite.config.js).

# VITE_API_URL=http://localhost:3001/api
